# 公开数据面恢复说明

真实事故记录和节点身份已迁移到私有运营仓库。本文件只保留通用恢复顺序。

1. 关闭客户端隧道，分别测试两方向的 TCP/UDP 基线。
2. 检查 MTU/PMTU、TCPMSS、丢包、抖动和单流/多流差异。
3. 如需中继，先验证同源转发和 UDP 会话策略，再切换客户端端点。
4. 重新执行完整负载、DNS、端点身份和出口身份验收。

恢复不得通过把真实地址、客户端配置或临时凭据写回公开仓库完成。
