# 公开首次部署流程

本流程只描述可公开的首次部署步骤；真实主机、账号、IP、端口拓扑和事故记录已迁移到私有运营仓库。

1. 使用仓库外配置文件运行校验和主机预检。
2. 部署后执行控制面、WireGuard 和数据面检查。
3. 以占位符端点运行真实客户端验收，分别确认端点身份与出口身份。
4. 保存原始验收输出到私有运维资料，不提交到公开仓库。

任何一项关键证据缺失，都应保持 `BLOCKED`，而不是用握手或单个 HTTP 请求推断服务完成。
