# 公开 GOST 中继说明

本文描述通用的同源 GOST 中继边界，不记录真实源站、区域、地址或链路测量结果。真实命令和验收记录在私有运营仓库。

- 生产配置只转发 WireGuard 所需端口。
- 资格测试阶段才临时开放诊断端口。
- UDP 会话必须显式声明 keepalive、TTL 和缓冲区策略。
- 中继通过参数接收源站地址，不允许在脚本中硬编码生产地址。
- 生产切换前必须完成双向 TCP/UDP 和真实客户端验收。
